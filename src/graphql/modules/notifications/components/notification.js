class Notification {
  constructor(notification_for, notification_type, information) {
    this.notification_for = notification_for;
    this.notification_type = notification_type;
    this.information = information;
  }
}

module.exports = Notification;
