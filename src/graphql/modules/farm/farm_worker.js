class FarmWorker {
  constructor(farm_id, user_id, invitation_code) {
    this.farm_id = farm_id;
    this.user_id = user_id;
    this.invitation_code = invitation_code;
  }
}

module.exports = FarmWorker;
