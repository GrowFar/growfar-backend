class Farm {
  constructor(name, user_id, address, longitude, latitude) {
    this.name = name;
    this.user_id = user_id;
    this.address = address;
    this.longitude = longitude;
    this.latitude = latitude;
  }
}

module.exports = Farm;
